!!! 5
html(ng-app='admin-app')
  head
    title= title
    != stylesheetLinkTag('app/application', 'vendor/bootstrap.min', 'vendor/bootstrap-theme.min')
    != javascriptIncludeTag('vendor/angular-min', 'vendor/angular-resource-min', 'vendor/jquery-min', 'vendor/bootstrap', 'app/admin')
    != csrfMetaTag()
  body
    .navbar.navbar-inverse
      .container
        .navbar-header
          a.navbar-brand(href='/', target='_self') itachi presentation

    .container
      - var flash = request.flash('info').pop(); if (flash) {
        .alert.alert-info= flash
      -   }

      - flash = request.flash('error').pop(); if (flash) {
        .alert.alert-error= flash
      - }

      != body

      hr
      footer
        p © 2012
